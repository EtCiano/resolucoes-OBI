# # Taxa
# primeiraLinha = input().split(' ')
# quantidade = int(primeiraLinha[0])
# taxa = float(primeiraLinha[1])
# lotes = [int(x) for x in input().split(' ')]
# lotes.sort()

# impostoTotal = 0
# while True:
#   maior = lotes.pop()
#   lotesDivisao = [maior, sum(lotes)]
#   print(lotes, lotesDivisao)
#   impostoTotal += max(lotesDivisao) * taxa
#   if len(lotes) == 1:
#     break

# print(impostoTotal)
